class ApplicationController < ActionController::Base
    #before_action :set_surendra, only: %i[ index ]
	# private
	#  def current_user
    # @_current_user ||= session[:current_user_id] &&
    #   User.find_by(email: session[:current_user_id])
    # end
end
